export const topla = (a,b) => {
    return a+b;
}

export const hello = (message) => {
    console.log(message);
};

const user = {
    "name": "Ahmet",
    "lastname": "yıldız"
};

export default user ;